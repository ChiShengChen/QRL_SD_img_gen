# 基礎配置
# @package _global_

# 設備配置
device: "cuda"
seed: 42
deterministic: true

# 數據配置
data:
  batch_size: 32
  num_workers: 4
  pin_memory: true

# 日誌配置
logging:
  level: "INFO"
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
  use_tensorboard: true
  log_dir: "logs"
