# CIFAR-10 數據集配置
# @package dataset

name: "cifar10"
num_classes: 10
image_size: 32
channels: 3

# 數據路徑
data_dir: "data/cifar10"
download: true

# 數據增強
augmentation:
  horizontal_flip: true
  random_crop: true
  normalize:
    mean: [0.485, 0.456, 0.406]
    std: [0.229, 0.224, 0.225]

